//
// Created by unknown on 22/05/19.
//

#include "Stack.h"
/* -------------------------------------------------------------------------------------------------------/
 *  Next - Gets the next item in the stack, if not stacked then get the first item
 * ----------------------------------------------------------------------------------------------------*/
void Stack::next() {
    if (stacked) {
        // select next stack
    } else {
        // use the first stack
    }
}

/* -------------------------------------------------------------------------------------------------------/
 *  Add - Adds a handler pointer to the stack list
 * ----------------------------------------------------------------------------------------------------*/
void Stack::add(Handler Handler) {

}

/* -------------------------------------------------------------------------------------------------------/
 *  Removes - Removes a handler pointer to the stack list
 * ----------------------------------------------------------------------------------------------------*/
void Stack::remove(Handler Handler) {

}

